{
  "name": "open-test-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first integration test sandbox for template-driven workflows, API cases, fixtures, and evidence.",
  "scripts": {
    "test": "go test ./..."
  }
}
